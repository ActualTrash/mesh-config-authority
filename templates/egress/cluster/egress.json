{
    "zone_key": "zone-default-zone",
    "cluster_key": "{{ .name }}-to-{{ .egress.host }}-cluster",
    "name": "{{ .egress.host }}",
    "instances": [],
    "require_tls": true,
    "secret": {
      "secret_key": "{{ .name }}.identity",
      "secret_name": "spiffe://quickstart.greymatter.io/{{ .name }}",
      "secret_validation_name": "spiffe://quickstart.greymatter.io",
      "subject_names": [
        "spiffe://quickstart.greymatter.io/{{ .egress.host }}"
      ],
      "ecdh_curves": [
        "X25519:P-256:P-521:P-384"
      ]
    }
}

