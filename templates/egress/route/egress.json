{
    "zone_key": "zone-default-zone",
    "domain_key": "{{ .name }}-egress-domain",
    "route_key": "{{ .name }}-to-{{ .egress.host }}-route",
    "path": "/{{ .egress.host }}/",
    "prefix_rewrite": "/",
    "shared_rules_key": "{{ .name }}-to-{{ .egress.host }}-rules"
  }
