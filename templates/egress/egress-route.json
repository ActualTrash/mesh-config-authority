{
    "zone_key": "zone-default-zone",
    "domain_key": "{{ .Name }}-egress-domain",
    "route_key": "{{ .Name }}-to-{{ .Egress.Name }}-route",
    "path": "/{{ .Egress.Name }}/",
    "prefix_rewrite": "/",
    "shared_rules_key": "{{ .Name }}-to-{{ .Egress.Name }}-rules"
  }
