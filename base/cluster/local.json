{
    "zone_key": "zone-default-zone",
    "cluster_key": "cluster-{{ .Name }}-local",
    "name": "{{ .Name }}-local",
    "instances": [
        {
            "host": "localhost",
            "port": "{{ .ServicePort }}"
        }
    ],
    "require_tls": false
}
